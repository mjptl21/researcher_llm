fastapi>=0.136              # needs starlette>=1.x pulled in by openhands-sdk
uvicorn[standard]>=0.31.1   # mcp (via openhands-sdk) requires uvicorn>=0.31.1
sse-starlette==2.1.3
python-dotenv>=1.1.0          # fastmcp (via openhands-sdk) requires python-dotenv>=1.1.0
pydantic>=2.12.5             # openhands-sdk 1.28+ requires pydantic>=2.12.5
openhands-sdk==1.28.1        # agent harness — runs the agents (openhands_runner.py)
ddgs>=9.0                    # DuckDuckGo search — web_search tool (no API key needed)
